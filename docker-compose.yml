services:
  dashboard:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - router-data:/data
    environment:
      - ASUSROUTERCONTROL_DATA_DIR=/data
    restart: unless-stopped

volumes:
  router-data:
